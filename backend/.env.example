# CORS — comma-separated full origins (protocol + domain). Required for Vercel/frontend.
# Examples: https://aurmarentals.com,https://www.aurmarentals.com,https://your-project.vercel.app
# CORS_ORIGINS=http://localhost:3000,http://localhost:5173,http://127.0.0.1:3000

# Quote email — REQUIRED for quote to reach your inbox
# Get your API key at https://resend.com/api-keys
# Without this, quotes save to DB but you will NOT receive emails
RESEND_API_KEY=your_resend_api_key_here

# Your inbox — where quote notifications are sent (default: arumaeventsservices@gmail.com)
# QUOTE_RECIPIENT_EMAIL=your-email@example.com

# Sender for outgoing emails. Resend test domain works without verification.
# For production, verify your domain at https://resend.com/domains
# EMAIL_FROM=Aruma Events <onboarding@resend.dev>
