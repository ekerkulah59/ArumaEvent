# Backend API base URL (no /api suffix). Used when not using mock data.
# Local: REACT_APP_BACKEND_URL=http://localhost:8000
# Production (Netlify): set REACT_APP_BACKEND_URL=https://arumaevent.onrender.com in dashboard

# Set to "true" only when running frontend without backend (mock mode).
# Omit or leave unset to use real API (default).
# REACT_APP_USE_MOCK_DATA=true

# Optional: Canonical and Open Graph URLs. Set in production so shares and search use the correct domain.
# REACT_APP_SITE_URL=https://www.aurmarentals.com
